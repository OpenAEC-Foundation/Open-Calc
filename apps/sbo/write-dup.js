var fs=require("fs");var p="C:/Users/MartijnvanderKolk/Documents/GitHub/Open-Calc/apps/sbo/src/app/api/projects/[id]/estimates/[estimateId]/duplicate/route.ts";var c="import { NextResponse } from \"next/server\";\nimport { getDefaultUserId } from \"@/lib/auth\";\nimport { prisma } from \"@/lib/db\";\n\n// POST: Duplicate/copy an estimate to the same or another project\nexport async function POST(\n  request: Request,\n  { params }: { params: Promise<{ id: string; estimateId: string }> }\n) {\n  try {\n    const userId = await getDefaultUserId();\n    const { id: projectId, estimateId } = await params;";
fs.writeFileSync(p,c);console.log("wrote part1");